<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo03"
            aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="{{ path('home') }}">Boutique</a>

    <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('product_list') }}">Produits</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('product_create') }}">Ajouter un produit</a>
                </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav mt-2 mt-lg-0">
            {% if not app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sign_in') }}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sign_up') }}">Créer un compte</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('cart_list') }}">Panier</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sign_out') }}">Deconnexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
